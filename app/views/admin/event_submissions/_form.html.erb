<%= form_with(model: event_submission) do |form| %>
  <% if event_submission.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(event_submission.errors.count, "error") %> prohibited this event_submission from being saved:</h2>

      <ul>
        <% event_submission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
