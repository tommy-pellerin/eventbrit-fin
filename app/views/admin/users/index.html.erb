<div class="container">
  <h1>Admin::Users#index</h1>
  <p>Voici la liste des utilisateurs : </p>
    <% i = 0 %>
    <% @users.each do |user| %>
    <%= i += 1 %> - 
    id : <%= user.id %> |
    first_name : <%= user.first_name %> |
    email : <%= user.email %> | <br>
    <%= link_to "edit", edit_admin_user_path(user.id), class:"btn btn-primary"  %>
    <%= button_to "supprimer", admin_user_path(user), method: :delete,data: { confirm: "Êtes-vous sûr de vouloir supprimer ce like ?" }, class: "btn btn-danger"  %>
    <br>
    <% end %>
</div>
